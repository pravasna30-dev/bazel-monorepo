# Use local Java toolchain (Apple JDK)
# Note: Currently using Apple JDK 21. To upgrade to Java 25, see JAVA_UPGRADE.md
build --java_language_version=21
build --java_runtime_version=local_jdk
build --tool_java_runtime_version=local_jdk

# Coverage config - use with: bazel coverage --config=coverage //...
# Keeps coverage flags separate to avoid discarding analysis cache during regular builds
build:coverage --collect_code_coverage
build:coverage --instrumentation_filter=//...

